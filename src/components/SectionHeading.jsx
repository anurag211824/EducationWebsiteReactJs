import React from 'react'

const SectionHeading = ({heading,subHeading}) => {
  return (
    <div>
      <p>{heading}</p>
      <h3>{subHeading}</h3>
    </div>
  )
}

export default SectionHeading